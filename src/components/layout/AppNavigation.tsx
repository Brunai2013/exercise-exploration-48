import React from 'react';

// This component is now deprecated as all navigation is handled by the Navbar component
// We're keeping it as an empty component to prevent any potential import errors

const AppNavigation: React.FC = () => {
  return null;
};

export default AppNavigation;
